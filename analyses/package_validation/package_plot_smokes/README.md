# Package Plot Smokes

Opt-in plot output smoke tests that were removed from the package default test suite. Run these directly only when changing plot-producing code or analysis output helpers. Their retained artifacts now follow the figure-owned `figures/<figure_id>/output/` layout.
