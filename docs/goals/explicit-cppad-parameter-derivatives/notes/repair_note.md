# Task B Scope Repair Note

Timestamp: 2026-05-13 America/Denver

The coordinator repaired Task B's local GoalBuddy automation paths and tightened the scope contract.

Task B must not end with only a vague `k_hb_ij` recorded limitation and no PR. The active PM task must now force one of these concrete end states:

- `PR_OPENED`: open a focused PR from the validated implementation with truthful coverage/capability language.
- `BLOCKED_SCOPE_GAP`: if the PR cannot truthfully preserve the `k_hb_ij` boundary, stop with an exact handoff.

Required `k_hb_ij` handling:

- Include `k_hb_ij` in coverage/capability.
- If full active-association property/regression derivatives are unavailable, classify it as `blocker_requires_implicit_association_sensitivity`.
- Add or update a test that prevents silent omission or a production-support overclaim.
- Identify Task C as the owner of implicit association site-fraction sensitivity.

Forbidden ending:

```text
k_hb_ij recorded as limitation. No PR opened.
```
