# Assumptions

- Slug chosen: `explicit-cppad-parameter-derivatives`.
- Task B has no issue URL in `docs/roadmaps/agent_prompts/index.yaml`, so the board records the issue as `null` until an issue exists.
- Task A dependency evidence is based on the current GitHub PR state and local remote refs at Goal Prep time.
- Because Task A is not merged, the branch rebase check was not used as a permission to implement.
